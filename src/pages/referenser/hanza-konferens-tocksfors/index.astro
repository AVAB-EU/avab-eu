---
import { getCollection } from "astro:content";
import ReferencePage from "../../../layouts/ReferencePage.astro";

const reference = (await getCollection("references")).find(
  (entry) => entry.data.slug === "/referenser/hanza-konferens-tocksfors/",
);

if (!reference) {
  throw new Error("Referensen Hanza Mechanics saknas i content collection.");
}
---

<ReferencePage reference={reference} />
