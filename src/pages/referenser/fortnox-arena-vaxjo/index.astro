---
import { getCollection } from "astro:content";
import ReferencePage from "../../../layouts/ReferencePage.astro";

const reference = (await getCollection("references")).find(
  (entry) => entry.data.slug === "/referenser/fortnox-arena-vaxjo/",
);

if (!reference) {
  throw new Error("Reference content missing for /referenser/fortnox-arena-vaxjo/");
}
---

<ReferencePage reference={reference} />
