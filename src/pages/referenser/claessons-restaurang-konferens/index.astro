---
import { getCollection } from "astro:content";
import ReferencePage from "../../../layouts/ReferencePage.astro";

const reference = (await getCollection("references")).find(
  (entry) => entry.data.slug === "/referenser/claessons-restaurang-konferens/",
);

if (!reference) {
  throw new Error("Referensen Claessons Konferens & Restaurang saknas i content collection.");
}
---

<ReferencePage reference={reference} />
