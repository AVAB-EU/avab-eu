---
import { getCollection } from "astro:content";
import ReferencePage from "../../../layouts/ReferencePage.astro";

const reference = (await getCollection("references")).find(
  (entry) => entry.data.slug === "/referenser/stockfallets-skola-karlstad/",
);

if (!reference) {
  throw new Error("Reference content missing for /referenser/stockfallets-skola-karlstad/");
}
---

<ReferencePage reference={reference} />
