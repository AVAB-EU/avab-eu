---
import { getCollection } from "astro:content";
import ReferencePage from "../../../layouts/ReferencePage.astro";

const reference = (await getCollection("references")).find(
  (entry) => entry.data.slug === "/referenser/friskis-solstadens-sportcenter/",
);

if (!reference) {
  throw new Error("Referensen Friskis&Svettis Karlstad saknas i content collection.");
}
---

<ReferencePage reference={reference} />
