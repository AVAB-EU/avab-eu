---
import { getCollection } from "astro:content";
import ReferencePage from "../../../layouts/ReferencePage.astro";

const reference = (await getCollection("references")).find(
  (entry) => entry.data.slug === "/referenser/skolhagenskolan-taby/",
);

if (!reference) {
  throw new Error("Reference content missing for /referenser/skolhagenskolan-taby/");
}
---

<ReferencePage reference={reference} />
